import 'package:flutter/material.dart';

class KeysmithPage extends StatelessWidget {
  static const routeName = "/keysmith";
  const KeysmithPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("KeySmith"));
  }
}
